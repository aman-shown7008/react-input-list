import React from 'react';
import ReactDOM from 'react-dom';
import InputList from 'react-input-list';

ReactDOM.render(
  <React.StrictMode>
    <InputList />
  </React.StrictMode>,
  document.getElementById('root')
);
